{
  "name": "music-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "check:openapi": "bun run scripts/check-openapi.ts",
    "check:service-refs": "bun run scripts/check-service-refs.ts",
    "check": "bun run check:openapi && bun run check:service-refs"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "ulid": "^2.3.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
